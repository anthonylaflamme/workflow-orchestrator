# Shared Workflow Note

Per-PRD ticket indexes no longer live here.

The orchestrator should create and maintain:

- `workflow/workspaces/<prd-slug>/TICKET-INDEX.md`

Use that per-PRD file as the canonical execution order for a specific backlog. Keep this root file only as a marker that the shared workflow core and PRD-specific outputs are intentionally separated.
