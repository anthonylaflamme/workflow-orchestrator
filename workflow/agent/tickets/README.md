# Shared Workflow Note

PRD-specific tickets no longer live in this root folder.

The orchestrator should create tickets under:

- `workflow/workspaces/<prd-slug>/tickets/`

Each workspace ticket folder should contain one ExecPlan per feature or substantial change, and each file must follow `workflow/agent/PLANS.MD`.
