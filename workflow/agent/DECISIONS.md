# Shared Workflow Note

Per-PRD decision logs no longer live here.

The orchestrator should create and maintain:

- `workflow/workspaces/<prd-slug>/DECISIONS.md`

Use that per-PRD file to record decisions that affect multiple tickets in the same PRD workspace. Keep this root file only as a marker that the shared workflow core and PRD-specific outputs are intentionally separated.
